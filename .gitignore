# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
*.tsbuildinfo
coverage

# Env files
.env
.env.*
!.env.example

# Secretos que NO viven en ningún respaldo ni en Supabase fuera de su panel.
# Ya los cubre la regla .env.* de arriba; van explícitos porque perderlos duele:
#   .env.secret       — IP_HASH_PEPPER de la edge function redeem-keyword
#   .env.backup.local — cadena de conexión a Postgres para `npm run db:backup`
.env.secret
.env.backup.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
Thumbs.db
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.vercel

# Claude Code local (personal) settings
.claude/settings.local.json

# Installed skill packages — reproducible via skills-lock.json
# (`npx skills experimental_install` restores them from the lockfile)
.claude/skills/
.agents/skills/

# Respaldos de la base de datos (contienen datos personales y hashes de contraseña)
backups/*
!backups/README.md
